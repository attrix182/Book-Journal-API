# ============================================
# Book Journal API - Variables de Entorno
# ============================================
# Copia este archivo a .env y completa con tus valores reales
# NO subas el archivo .env al repositorio (debe estar en .gitignore)

# ============================================
# FIREBASE CONFIGURATION (OBLIGATORIO)
# ============================================
# Configuración completa de Firebase Admin SDK como JSON string
# En producción (Render), pega aquí el JSON completo de tu service account
# En desarrollo local, puedes usar firebase-config.json en su lugar
# Ejemplo: {"type":"service_account","project_id":"tu-proyecto",...}
FIREBASE_CONFIG=

# ============================================
# SMTP CONFIGURATION (OPCIONAL - Solo si usas notificaciones por email)
# ============================================
# Host del servidor SMTP
# Ejemplos:
#   - Gmail: smtp.gmail.com
#   - Outlook: smtp-mail.outlook.com
#   - Mailgun: smtp.mailgun.org
#   - SendGrid: smtp.sendgrid.net
SMTP_HOST=

# Puerto SMTP (opcional, default: 587)
# 587 = TLS (recomendado)
# 465 = SSL
SMTP_PORT=587

# Usuario/Email para autenticación SMTP
# Ejemplo para Gmail: tu-email@gmail.com
# Ejemplo para SendGrid: apikey
SMTP_USER=

# Contraseña o token de aplicación SMTP
# Para Gmail: usa una "Contraseña de aplicación" (no tu contraseña normal)
# Genera una en: https://myaccount.google.com/apppasswords
SMTP_PASSWORD=

# Email remitente (opcional, default: usa SMTP_USER)
# El email que aparecerá como "De:" en los correos enviados
SMTP_FROM=

# Usar conexión segura SSL (opcional, default: false)
# true = Puerto 465 (SSL)
# false = Puerto 587 (TLS) - Recomendado
SMTP_SECURE=false

# ============================================
# SERVER CONFIGURATION (OPCIONAL)
# ============================================
# Puerto donde correrá el servidor (opcional, default: 3000)
# En Render, esto se configura automáticamente
PORT=3000

# Entorno de ejecución (opcional)
# production = Modo producción
# development = Modo desarrollo
NODE_ENV=production

# ============================================
# NOTAS IMPORTANTES
# ============================================
# 1. FIREBASE_CONFIG es OBLIGATORIO para que la API funcione
# 2. Las variables SMTP son OPCIONALES - solo necesarias si quieres enviar emails
#    Si no configuras SMTP, el sistema seguirá funcionando pero solo enviará notificaciones push
# 3. En Render, configura estas variables en:
#    Dashboard > Tu Servicio > Environment > Add Environment Variable
# 4. Para desarrollo local, crea un archivo .env en la raíz del proyecto
# 5. NUNCA subas el archivo .env al repositorio (debe estar en .gitignore)
